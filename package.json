{
  "name": "the-game-lib",
  "version": "1.0.11",
  "description": "Class library for The Game",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "tsc": "tsc -p tsconfig.json",
    "test": "jest --watchAll",
    "coverage": "jest --coverage",
    "prepublishOnly": "npm run tsc"
  },
  "author": "phrasmotica",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "game-server-lib": "^1.0.2"
  }
}
